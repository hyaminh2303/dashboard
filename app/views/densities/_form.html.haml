/ /.box-header
/ form start
.box.box-primary
  .box-header
    %h3.box-title
    = simple_form_for(@density) do |f|
      .box-body.form-inputs
        = render :partial => '/shared/errors', locals: {model: @density}
        = f.input :country_code
        = f.input :cpc
        = f.input :cpm
        = f.input :population

        = f.button :submit, :class => 'btn btn-primary'
        = link_to t('views.form.cancel'), densities_path, :class => 'btn btn-default'
